import React from 'react';
import ProtectedPlaceholder from '../components/ProtectedPlaceholder';

const Resource = () => {
  return (
    <ProtectedPlaceholder
      title="Resources"
      description="This protected Resource page is ready. Next step is wiring resource upload/list APIs and module-based filtering."
    />
  );
};

export default Resource;
