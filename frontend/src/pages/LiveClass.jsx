import React from 'react';
import ProtectedPlaceholder from '../components/ProtectedPlaceholder';

const LiveClass = () => {
  return (
    <ProtectedPlaceholder
      title="Live Class"
      description="This protected Live Class page is a placeholder for schedules, links, and attendance features."
    />
  );
};

export default LiveClass;
